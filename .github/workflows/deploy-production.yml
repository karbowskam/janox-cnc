name: Build and Deploy
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.1

      - name: Install Node
        uses: actions/setup-node@v1

      - name: Install project
        run: npm install .

      - name: Test and Lint
        run: npm run test

      - name: build-and-deploy
        run: npm run build

      - name: Deploy to gh-pages
        run: npm run publish-cmd
